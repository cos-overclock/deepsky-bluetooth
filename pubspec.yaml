name: deepsky_bluetooth
description: "Cross-platform BLE (Bluetooth Low Energy) client for Flutter with a device-oriented API: scanning, connection with auto-reconnect, and GATT operations. Supports foreground and background use on Android, iOS, and macOS."
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

workspace:
  - packages/deepsky_bluetooth_util
  - packages/deepsky_bluetooth_interface
  - packages/deepsky_bluetooth_android_bridge
  - packages/deepsky_bluetooth_ios_bridge
  - packages/deepsky_bluetooth_macos_bridge
  - plugins/deepsky_bluetooth_android
  - plugins/deepsky_bluetooth_ios
  - plugins/deepsky_bluetooth_macos

dependencies:
  flutter:
    sdk: flutter

  steady: ^1.2.0
  deepsky_bluetooth_interface:
    path: packages/deepsky_bluetooth_interface
  deepsky_bluetooth_android_bridge:
    path: packages/deepsky_bluetooth_android_bridge
  deepsky_bluetooth_ios_bridge:
    path: packages/deepsky_bluetooth_ios_bridge
  deepsky_bluetooth_macos_bridge:
    path: packages/deepsky_bluetooth_macos_bridge

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
