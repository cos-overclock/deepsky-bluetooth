name: deepsky_bluetooth_util
description: "Shared UUID and device identifier value types for deepsky_bluetooth."
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
