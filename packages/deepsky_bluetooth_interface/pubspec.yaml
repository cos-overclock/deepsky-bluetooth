name: deepsky_bluetooth_interface
description: "Platform-agnostic interface for deepsky_bluetooth: shared models, error types, and the abstract platform contract implemented by the per-platform bridges."
version: 0.0.1
publish_to: 'none'
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
